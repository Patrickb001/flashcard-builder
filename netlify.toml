[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Single-page app: every non-API route serves the built index.html.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
